// Função padrão
function soma (num1, num2) {
  return console.log(num1 + num2);
}

// Arrow Function
const soma = (num1, num2) => {
  return console.log(num1 + num2);
};

// Com um código que vai apenas uma linha
const soma = (num1, num2) => console.log(num1 + num2);

soma(1, 2);
